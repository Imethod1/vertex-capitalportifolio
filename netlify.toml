[build]
  command = "npm run build"
  publish = "dist"

# Ensure Decap CMS admin loads its own index, not SPA fallback
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200

# SPA fallback for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
